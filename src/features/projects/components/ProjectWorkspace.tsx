'use client'

import Link from 'next/link'
import { ChevronLeft, ChevronRight, FolderKanban, Plus, ShieldAlert, Timer, TrendingUp } from 'lucide-react'
import type { Project } from '@/features/projects/types/project'
import { useProjectFilters } from '@/features/projects/hooks/useProjectFilters'
import { ProjectCard } from '@/features/projects/components/ProjectCard'
import { ProjectFilters } from '@/features/projects/components/ProjectFilters'
import { ProjectSearch } from '@/features/projects/components/ProjectSearch'
import { ProjectTable } from '@/features/projects/components/ProjectTable'
import { Card } from '@/components/ui/Card'
import { EmptyState } from '@/components/ui/EmptyState'

const PAGE_SIZE = 6

type ProjectWorkspaceProps = { projects: Project[] }

export function ProjectWorkspace({ projects }: ProjectWorkspaceProps) {
  const { query, filters, sortKey, sortDirection, page, filteredProjects, setPage, updateQuery, updateFilters, toggleSort, clearFilters } = useProjectFilters(projects)
  const totalPages = Math.max(1, Math.ceil(filteredProjects.length / PAGE_SIZE))
  const visibleProjects = filteredProjects.slice((page - 1) * PAGE_SIZE, page * PAGE_SIZE)
  const hasActiveFilters = Boolean(query || filters.status !== 'all' || filters.projectType !== 'all')
  const activeProjects = projects.filter((project) => project.status === 'Active').length
  const atRiskProjects = projects.filter((project) => project.status === 'At Risk').length
  const planningProjects = projects.filter((project) => project.status === 'Planning').length

  return <div className="mx-auto w-full max-w-[1600px] space-y-7"><header className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[#7187ff]">Operations workspace</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.05em] text-white sm:text-4xl">Construction projects</h1><p className="mt-2 max-w-xl text-sm text-[#9CA3AF]">Track delivery, budgets, site activity, and the people moving every project forward.</p></div><Link href="/projects/new" className="inline-flex h-11 items-center justify-center gap-2 rounded-xl bg-[#163DFF] px-4 text-xs font-semibold text-white shadow-[0_10px_25px_rgba(22,61,255,0.2)] transition hover:bg-[#3155ff] hover:shadow-[0_14px_30px_rgba(22,61,255,0.3)] focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-[#163DFF]/30"><Plus size={16} /> New project</Link></header><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Total projects" value={projects.length.toString()} detail="Across all project stages" icon={FolderKanban} /><MetricCard label="Active projects" value={activeProjects.toString()} detail="Currently in delivery" icon={TrendingUp} accent="emerald" /><MetricCard label="Projects at risk" value={atRiskProjects.toString()} detail="Require attention" icon={ShieldAlert} accent="amber" /><MetricCard label="In planning" value={planningProjects.toString()} detail="Preparing to start" icon={Timer} accent="sky" /></div><Card className="p-5 sm:p-6"><div className="flex flex-col gap-4 lg:flex-row lg:items-center"><ProjectSearch value={query} onChange={updateQuery} /><span className="text-xs text-[#646873]">{filteredProjects.length} project{filteredProjects.length === 1 ? '' : 's'}</span></div><ProjectFilters value={filters} onChange={updateFilters} onClear={clearFilters} hasActiveFilters={hasActiveFilters} /></Card>{visibleProjects.length ? <><ProjectTable projects={visibleProjects} sortKey={sortKey} sortDirection={sortDirection} onSort={toggleSort} /><div className="grid gap-4 md:grid-cols-2 lg:hidden">{visibleProjects.map((project) => <ProjectCard project={project} key={project.id} />)}</div><div className="flex flex-col items-center justify-between gap-3 text-xs text-[#646873] sm:flex-row"><span>Showing {((page - 1) * PAGE_SIZE) + 1}-{Math.min(page * PAGE_SIZE, filteredProjects.length)} of {filteredProjects.length}</span><div className="flex items-center gap-2"><button type="button" onClick={() => setPage((currentPage) => Math.max(1, currentPage - 1))} disabled={page === 1} className="inline-flex h-9 items-center gap-1 rounded-lg border border-white/[0.08] px-3 text-xs font-medium text-[#9CA3AF] transition hover:bg-white/[0.05] hover:text-white disabled:cursor-not-allowed disabled:opacity-40"><ChevronLeft size={14} /> Previous</button><span className="px-2 text-[#9CA3AF]">Page {page} of {totalPages}</span><button type="button" onClick={() => setPage((currentPage) => Math.min(totalPages, currentPage + 1))} disabled={page === totalPages} className="inline-flex h-9 items-center gap-1 rounded-lg border border-white/[0.08] px-3 text-xs font-medium text-[#9CA3AF] transition hover:bg-white/[0.05] hover:text-white disabled:cursor-not-allowed disabled:opacity-40">Next <ChevronRight size={14} /></button></div></div></> : <EmptyState icon={FolderKanban} title="No projects found" description={hasActiveFilters ? 'Try adjusting your search or filters to find a matching project.' : 'Your project portfolio is ready for its first record.'} action={hasActiveFilters ? <button type="button" onClick={clearFilters} className="text-xs font-semibold text-[#7187ff] transition hover:text-white">Clear search and filters</button> : <Link href="/projects/new" className="inline-flex h-10 items-center gap-2 rounded-lg bg-[#163DFF] px-4 text-xs font-semibold text-white transition hover:bg-[#3155ff]"><Plus size={14} /> Add project</Link>} />}</div>
}

type MetricCardProps = { label: string; value: string; detail: string; icon: typeof FolderKanban; accent?: 'blue' | 'emerald' | 'amber' | 'sky' }
function MetricCard({ label, value, detail, icon: Icon, accent = 'blue' }: MetricCardProps) { const accentClass = accent === 'emerald' ? 'bg-emerald-400/[0.1] text-emerald-300' : accent === 'amber' ? 'bg-amber-400/[0.1] text-amber-300' : accent === 'sky' ? 'bg-sky-400/[0.1] text-sky-300' : 'bg-[#163DFF]/[0.12] text-[#7187ff]'; return <Card className="p-5"><div className="flex items-start justify-between"><span className={`flex h-9 w-9 items-center justify-center rounded-xl ${accentClass}`}><Icon size={17} strokeWidth={1.8} /></span><span className="text-[10px] font-semibold uppercase tracking-[0.12em] text-[#646873]">PMO</span></div><p className="mt-5 text-2xl font-semibold tracking-[-0.04em] text-white">{value}</p><p className="mt-1 text-xs font-medium text-[#d5d7df]">{label}</p><p className="mt-2 text-[11px] text-[#646873]">{detail}</p></Card> }
